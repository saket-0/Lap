// --- DOM ELEMENTS ---
// Declare all variables with 'let' but do not assign them.
// We will assign them in main.js AFTER the DOM is loaded.
let loginOverlay, loginForm, loginEmailInput, loginEmailSelect, quickLoginButton,
    appWrapper, appContent, logoutButton, userNameEl, userRoleEl, userEmployeeIdEl,
    navLinks, errorToast, errorMessage, successToast, successMessage, templates;